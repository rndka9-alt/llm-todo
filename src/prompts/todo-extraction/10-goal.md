Goal:

- Extract actionable TODO items from the target block.
- Return conservative metadata when it is clearly supported by the text.
- Return a short exact `sourceQuote` from the target block for each TODO.
- Return strict JSON only.
