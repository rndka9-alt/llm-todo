Context rules:

- Context blocks may help resolve references like names, dates, or pronouns.
- Do not extract TODOs from context blocks.
- Do not copy `sourceQuote` from context blocks.
- If context conflicts with the target block, trust the target block.
- If context is unnecessary, ignore it.
